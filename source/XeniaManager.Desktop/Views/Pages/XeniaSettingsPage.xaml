<Page x:Class="XeniaManager.Desktop.Views.Pages.XeniaSettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="810"
      Title="XeniaSettingsPage">
    <Page.Resources>
        <Style x:Key="FlatMenuItemStyle" TargetType="MenuItem">
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <ContentPresenter ContentSource="Header" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="80" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0"
                      HorizontalAlignment="Stretch"
                      MinWidth="500"
                      Margin="5,0" />
            <Menu Grid.Column="1"
                  HorizontalAlignment="Stretch"
                  Margin="0,0,5,0">
                <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                    <MenuItem.Header>
                        <Button x:Name="BtnResetSettings"
                                Background="{x:Null}"
                                BorderBrush="{x:Null}"
                                Cursor="Hand"
                                FontSize="24"
                                VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="ArrowClockwise24" />
                        </Button>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                    <MenuItem.Header>
                        <Button x:Name="BtnOptimizeSettings"
                                Background="{x:Null}"
                                BorderBrush="{x:Null}"
                                Cursor="Hand"
                                FontSize="24"
                                VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="CloudSync24" />
                        </Button>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                    <MenuItem.Header>
                        <Button x:Name="BtnOpenInEditor"
                                Background="{x:Null}"
                                BorderBrush="{x:Null}"
                                Cursor="Hand"
                                FontSize="24"
                                VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="Pen24" />
                        </Button>
                    </MenuItem.Header>
                </MenuItem>
                <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                    <MenuItem.Header>
                        <Button x:Name="BtnSaveSettings"
                                Background="{x:Null}"
                                BorderBrush="{x:Null}"
                                Cursor="Hand"
                                FontSize="24"
                                VerticalAlignment="Center">
                            <ui:SymbolIcon Symbol="Save24" />
                        </Button>
                    </MenuItem.Header>
                </MenuItem>
            </Menu>
        </Grid>

        <Border Grid.Row="1"
                Background="{DynamicResource TextFillColorSecondaryBrush}"
                Height="1" />

        <Menu Grid.Row="2"
              HorizontalAlignment="Center"
              VerticalAlignment="Center"
              Margin="5">
            <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                <MenuItem.Header>
                    <Button x:Name="BtnAudioSettings"
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            Cursor="Hand"
                            VerticalAlignment="Center"
                            Click="BtnShowSettings_Click">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <ui:SymbolIcon Symbol="SpeakerEdit24" FontSize="28" />
                            <TextBlock Text="{DynamicResource XeniaSettingsPage_AudioSettingsTitleText}"
                                       FontSize="18"
                                       Margin="0,4,0,0"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </MenuItem.Header>
            </MenuItem>
            <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                <MenuItem.Header>
                    <Button x:Name="BtnDisplaySettings"
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            Cursor="Hand"
                            FontSize="36"
                            VerticalAlignment="Center"
                            Click="BtnShowSettings_Click">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <ui:SymbolIcon Symbol="Desktop24" FontSize="28" />
                            <TextBlock Text="{DynamicResource XeniaSettingsPage_DisplaySettingsTitleText}"
                                       FontSize="18"
                                       Margin="0,4,0,0"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </MenuItem.Header>
            </MenuItem>
            <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                <MenuItem.Header>
                    <Button x:Name="BtnGraphicalSettings"
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            Cursor="Hand"
                            FontSize="36"
                            VerticalAlignment="Center"
                            Click="BtnShowSettings_Click">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <ui:SymbolIcon Symbol="DrawImage24" FontSize="28" />
                            <TextBlock Text="{DynamicResource XeniaSettingsPage_GraphicalSettingsTitleText}"
                                       FontSize="18"
                                       Margin="0,4,0,0"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </MenuItem.Header>
            </MenuItem>
            <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                <MenuItem.Header>
                    <Button x:Name="BtnGeneralSettings"
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            Cursor="Hand"
                            FontSize="36"
                            VerticalAlignment="Center"
                            Click="BtnShowSettings_Click">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <ui:SymbolIcon Symbol="Globe24" FontSize="28" />
                            <TextBlock Text="{DynamicResource XeniaSettingsPage_GeneralSettingsTitleText}"
                                       FontSize="18"
                                       Margin="0,4,0,0"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </MenuItem.Header>
            </MenuItem>
            <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                <MenuItem.Header>
                    <Button x:Name="BtnUserInputSettings"
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            Cursor="Hand"
                            FontSize="36"
                            VerticalAlignment="Center"
                            Click="BtnShowSettings_Click">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <ui:SymbolIcon Symbol="Cursor24" FontSize="28" />
                            <TextBlock Text="{DynamicResource XeniaSettingsPage_UserInputSettingsTitleText}"
                                       FontSize="18"
                                       Margin="0,4,0,0"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </MenuItem.Header>
            </MenuItem>
            <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                <MenuItem.Header>
                    <Button x:Name="BtnStorageSettings"
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            Cursor="Hand"
                            FontSize="36"
                            VerticalAlignment="Center"
                            Click="BtnShowSettings_Click">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <ui:SymbolIcon Symbol="Storage24" FontSize="28" />
                            <TextBlock Text="{DynamicResource XeniaSettingsPage_StorageSettingsTitleText}"
                                       FontSize="18"
                                       Margin="0,4,0,0"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </MenuItem.Header>
            </MenuItem>
            <MenuItem Style="{StaticResource FlatMenuItemStyle}">
                <MenuItem.Header>
                    <Button x:Name="BtnHackSettings"
                            Background="{x:Null}"
                            BorderBrush="{x:Null}"
                            Cursor="Hand"
                            FontSize="36"
                            VerticalAlignment="Center"
                            Click="BtnShowSettings_Click">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="28" />
                            <TextBlock Text="{DynamicResource XeniaSettingsPage_HackSettingsTitleText}"
                                       FontSize="18"
                                       Margin="0,4,0,0"
                                       HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </MenuItem.Header>
            </MenuItem>
        </Menu>

        <StackPanel x:Name="SpSettingsPanel"
                    Grid.Row="3"
                    ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                    ScrollViewer.VerticalScrollBarVisibility="Visible">
            <StackPanel x:Name="SpAudioSettings">
                <!-- apu -->
                <Border x:Name="BrdAudioSystemSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_AudioSystemSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_AudioSystemSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <ComboBox Grid.Column="1"
                                  x:Name="CmbAudioSystem"
                                  Cursor="Hand"
                                  FontSize="18"
                                  Margin="0,10"
                                  HorizontalAlignment="Center"
                                  MinWidth="150">
                            <ComboBox.Items>
                                <ComboBoxItem Content="any" />
                                <ComboBoxItem Content="nop" />
                                <ComboBoxItem Content="sdl" />
                                <ComboBoxItem Content="xaudio2" />
                            </ComboBox.Items>
                        </ComboBox>
                    </Grid>
                </Border>

                <!-- apu_max_queued_frames -->
                <Border x:Name="BrdAudioMaxQueuedFramesSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_AudioMaxQueuedFramesSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_AudioMaxQueuedFramesSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <TextBox x:Name="TxtAudioMaxQueuedFrames"
                                 Grid.Column="1"
                                 FontSize="18"
                                 HorizontalContentAlignment="Center"
                                 VerticalContentAlignment="Center"
                                 Margin="0,10"
                                 Width="130" />
                    </Grid>
                </Border>

                <!-- mute -->
                <Border x:Name="BrdAudioMuteSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_AudioMuteSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_AudioMuteSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <CheckBox x:Name="ChkAudioMute"
                                  Grid.Column="1"
                                  HorizontalAlignment="Center"
                                  Margin="0,10"
                                  Width="45" />
                    </Grid>
                </Border>

                <!-- enable_xmp -->
                <Border x:Name="BrdEnableXmpSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_AudioXmpSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_AudioXmpSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <CheckBox x:Name="ChkXmp"
                                  Grid.Column="1"
                                  HorizontalAlignment="Center"
                                  Margin="0,10"
                                  Width="45" />
                    </Grid>
                </Border>

                <!-- xmp_default_volume -->
                <Border x:Name="BrdXmpVolumeSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_AudioXmpVolumeSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_AudioXmpVolumeSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <Grid Grid.Column="1"
                              Margin="0"
                              VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Slider x:Name="SldXmpVolume"
                                    Grid.Row="0"
                                    Minimum="0" Maximum="100"
                                    IsSnapToTickEnabled="True" TickFrequency="1"
                                    Width="240" />
                            <TextBlock x:Name="TxtSldXmpVolume"
                                       Grid.Row="1"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                       Text="0" />
                        </Grid>
                    </Grid>
                </Border>

                <!-- use_dedicated_xma_thread -->
                <Border x:Name="BrdAudioDedicatedXmaThreadSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_AudioUseDedicatedXmaThreadSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_AudioUseDedicatedXmaThreadSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <CheckBox x:Name="ChkDedicatedXmaThread"
                                  Grid.Column="1"
                                  HorizontalAlignment="Center"
                                  Margin="0,10"
                                  Width="45" />
                    </Grid>
                </Border>

                <!-- use_new_decoder -->
                <Border x:Name="BrdAudioXmaDecoderSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_AudioXmaDecoderSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_AudioXmaDecoderSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <CheckBox x:Name="ChkXmaAudioDecoder"
                                  Grid.Column="1"
                                  HorizontalAlignment="Center"
                                  Margin="0,10"
                                  Width="45" />
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel x:Name="SpDisplaySettings">
                <!-- fullscreen -->
                <Border x:Name="BrdDisplayFullscreenSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_DisplayFullscreenSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_DisplayFullscreenSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <CheckBox x:Name="ChkFullscreen"
                                  Grid.Column="1"
                                  HorizontalAlignment="Center"
                                  Margin="0,10"
                                  Width="45" />
                    </Grid>
                </Border>

                <!-- internal_display_resolution -->
                <Border x:Name="BrdInternalDisplayResolutionSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_DisplayInternalResolutionSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_DisplayInternalResolutionSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <ComboBox Grid.Column="1"
                                  x:Name="CmbInternalDisplayResolution"
                                  Cursor="Hand"
                                  FontSize="18"
                                  HorizontalAlignment="Center"
                                  Margin="0,10"
                                  MinWidth="150">
                            <ComboBox.Items>
                                <ComboBoxItem Content="640x480" />
                                <ComboBoxItem Content="640x576" />
                                <ComboBoxItem Content="720x480" />
                                <ComboBoxItem Content="720x576" />
                                <ComboBoxItem Content="800x600" />
                                <ComboBoxItem Content="848x480" />
                                <ComboBoxItem Content="1024x768" />
                                <ComboBoxItem Content="1152x864" />
                                <ComboBoxItem Content="1280x720" />
                                <ComboBoxItem Content="1280x768" />
                                <ComboBoxItem Content="1280x960" />
                                <ComboBoxItem Content="1280x1024" />
                                <ComboBoxItem Content="1360x768" />
                                <ComboBoxItem Content="1440x900" />
                                <ComboBoxItem Content="1680x1050" />
                                <ComboBoxItem Content="1920x540" />
                                <ComboBoxItem Content="1920x1080" />
                                <ComboBoxItem Content="Custom" />
                            </ComboBox.Items>
                        </ComboBox>
                    </Grid>
                </Border>

                <!-- internal_display_resolution_x -->
                <Border x:Name="BrdCustomInternalDisplayResolutionWidthSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_DisplayCustomInternalResolutionWidthSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_DisplayCustomInternalResolutionWidthSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <TextBox x:Name="TxtCustomInternalDisplayResolutionWidth"
                                 Grid.Column="1"
                                 FontSize="18"
                                 HorizontalContentAlignment="Center"
                                 VerticalContentAlignment="Center"
                                 Margin="0,10"
                                 Width="130" />
                    </Grid>
                </Border>

                <!-- internal_display_resolution_y -->
                <Border x:Name="BrdCustomInternalDisplayResolutionHeightSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_DisplayCustomInternalResolutionHeightSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_DisplayCustomInternalResolutionHeightSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <TextBox x:Name="TxtCustomInternalDisplayResolutionHeight"
                                 Grid.Column="1"
                                 FontSize="18"
                                 HorizontalContentAlignment="Center"
                                 VerticalContentAlignment="Center"
                                 Margin="0,10"
                                 Width="130" />
                    </Grid>
                </Border>

                <!-- present_letterbox -->
                <Border x:Name="BrdDisplayLetterboxSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_DisplayLetterboxSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_DisplayLetterboxSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <CheckBox x:Name="ChkLetterbox"
                                  Grid.Column="1"
                                  HorizontalAlignment="Center"
                                  Margin="0,10"
                                  Width="45" />
                    </Grid>
                </Border>

                <!-- widescreen -->
                <Border x:Name="BrdDisplayWidescreenSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_DisplayWidescreenSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_DisplayWidescreenSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <CheckBox x:Name="ChkWidescreen"
                                  Grid.Column="1"
                                  HorizontalAlignment="Center"
                                  Margin="0,10"
                                  Width="45" />
                    </Grid>
                </Border>

                <!-- vsync -->
                <Border x:Name="BrdXeniaVerticalSyncSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_DisplayVSyncSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_DisplayVSyncSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <CheckBox x:Name="ChkXeniaVSync"
                                  Grid.Column="1"
                                  HorizontalAlignment="Center"
                                  Margin="0,10"
                                  Width="45" />
                    </Grid>
                </Border>

                <!-- framerate_limit -->
                <Border x:Name="BrdXeniaFramerateLimitSetting"
                        Height="70"
                        MaxWidth="810"
                        Style="{DynamicResource SettingBorder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0"
                                   Style="{DynamicResource SettingTextBlock}"
                                   Text="{DynamicResource XeniaSettingsPage_DisplayFramerateLimitSettingText}">
                            <TextBlock.ToolTip>
                                <TextBlock TextAlignment="Center"
                                           Text="{DynamicResource XeniaSettingsPage_DisplayFramerateLimitSettingTooltip}" />
                            </TextBlock.ToolTip>
                        </TextBlock>
                        <Grid Grid.Column="1"
                              Margin="0"
                              VerticalAlignment="Center">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <Slider x:Name="SldXeniaFramerate"
                                    Grid.Row="0"
                                    Minimum="0" Maximum="144"
                                    IsSnapToTickEnabled="True" TickFrequency="1"
                                    Width="240" />
                            <TextBlock x:Name="TxtSldXeniaFramerate"
                                       Grid.Row="1"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                       Text="Off" />
                        </Grid>
                    </Grid>
                </Border>
            </StackPanel>

            <StackPanel x:Name="SpGraphicalSettings">
                <ui:TextBlock x:Name="TxtGraphicalSettingsGroupTitle"
                              FontSize="40"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontWeight="Bold"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="0,10"
                              Text="Graphics" />
            </StackPanel>

            <StackPanel x:Name="SpGeneralSettings">
                <ui:TextBlock x:Name="TxtGeneralSettingsGroupTitle"
                              FontSize="40"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontWeight="Bold"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="0,10"
                              Text="General" />
            </StackPanel>

            <StackPanel x:Name="SpUserInputSettings">
                <ui:TextBlock x:Name="TxtUserInputSettingsGroupTitle"
                              FontSize="40"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontWeight="Bold"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="0,10"
                              Text="User Input" />
            </StackPanel>

            <StackPanel x:Name="SpStorageSettings">
                <ui:TextBlock x:Name="TxtStorageSettingsGroupTitle"
                              FontSize="40"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontWeight="Bold"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="0,10"
                              Text="Storage" />
            </StackPanel>

            <StackPanel x:Name="SpHackSettings">
                <ui:TextBlock x:Name="TxtHackSettingsGroupTitle"
                              FontSize="40"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              FontWeight="Bold"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="0,10"
                              Text="Hacks" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>